# Rejected draft calculation

The direct core-Jacobian probability bound in this directory is invalid and is
not part of the release.  It incorrectly multiplied constraint ranks across
public-form blocks even though those blocks contribute additively to the same
Jacobian columns.  The files are retained only as an internal audit trail and
must not be cited or shipped.
