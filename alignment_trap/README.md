# The Alignment Trap: Structural Limits of SNOVA-Style Matrix Signatures

This directory contains the complete standalone architecture paper. It does
not reproduce the concrete forgery paper; it cites that paper as the SNOVA
Version 2.3 application.

Build with:

```sh
make
```

The manuscript has exactly one LaTeX source file, `paper.tex`.
